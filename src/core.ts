type User = {
    name: string;
};

type Team = {
    members: User[];
    name: string;
}

type Repo = {
    name: string;
}

type Project = {
    name: string;
}

type Course = {
    name: string;
}